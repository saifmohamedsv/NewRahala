import { createGlobalStyle } from "styled-components"

export const GlboalStyles = createGlobalStyle`
*{
    font-family: 'Roboto', sans-serif;
    font-size:10px;
    margin:0;
    padding:0;
    box-sizing:border-box;
}
`
